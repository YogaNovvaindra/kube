apiVersion: apps/v1
kind: Deployment
metadata:
  name: vaultwarden
  namespace: tools
  labels:
    app: vaultwarden
  annotations:
    keel.sh/policy: force
    keel.sh/trigger: poll 
    keel.sh/match-tag: "true"
    keel.sh/pollSchedule: "@every 6h"
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: vaultwarden
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: vaultwarden
    spec:
      containers:
      - name: vaultwarden
        image: vaultwarden/server:latest@sha256-68e1b48cbd61e46b7cb670b6f0f0660f3fe8afdf7f3f74d66704e58f399ed04f
        imagePullPolicy: Always
        ports:
        - containerPort: 80
        volumeMounts:
        - name: vaultwarden-data
          mountPath: /data
      volumes:
      - name: vaultwarden-data
        hostPath:
          path: /mnt/cephfs/docker/tools/vaultwarden
          type: Directory
---
apiVersion: v1
kind: Service
metadata:
  name: vaultwarden
  namespace: tools
spec:
  type: LoadBalancer
  externalIPs:
    - 10.1.1.250
  selector:
    app: vaultwarden
  ports:
  - port: 8085
    targetPort: 80