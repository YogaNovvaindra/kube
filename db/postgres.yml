apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres
  namespace: db
  labels:
    app: postgres
spec:
  serviceName: postgres
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      nodeSelector:
        kubernetes.io/hperf: "true"
      terminationGracePeriodSeconds: 10
      containers:
        - name: postgres
          image: pgautoupgrade/pgautoupgrade:debian@sha256:6c5e519451fe7ad193678e8ad79bd9b258c8c70bbb43410a55ff3cd1a9ce0a5f
          imagePullPolicy: IfNotPresent
          command:
            - "sh"
            - "-c"
            - |
              set -e
              if ! dpkg -l | grep -q postgresql-.*-cron; then
                apt-get update
                for v in $(ls /usr/lib/postgresql/); do
                  apt-get install -y --no-install-recommends postgresql-${v}-cron || true
                done
                rm -rf /var/lib/apt/lists/*
              fi
              exec docker-entrypoint.sh postgres \
                -c shared_preload_libraries=pg_stat_statements,pg_cron \
                -c cron.database_name=firecrawl \
                -c max_slot_wal_keep_size=10GB
          resources:
            requests:
              memory: 256Mi
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: pg-cred
                  key: username
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: pg-cred
                  key: password
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql
          lifecycle:
            postStart:
              exec:
                command:
                  - "sh"
                  - "-c"
                  - 'until HBA=$(find /var/lib/postgresql -name pg_hba.conf 2>/dev/null | head -n 1) && [ -n "$HBA" ]; do sleep 2; done; grep -q "host replication all 0.0.0.0/0 scram-sha-256" "$HBA" || echo "host replication all 0.0.0.0/0 scram-sha-256" >> "$HBA"; su - postgres -c "pg_ctl reload -D $(dirname $HBA)" || true'
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "postgres"]
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 60
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "postgres"]
            initialDelaySeconds: 30
            periodSeconds: 60
            timeoutSeconds: 60
        - name: postgres-exporter
          image: prometheuscommunity/postgres-exporter:v0.20.1
          args:
            - "--auto-discover-databases"
            - "--collector.stat_statements"
          ports:
            - containerPort: 9187
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: pg-cred
                  key: username
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: pg-cred
                  key: password
            - name: DATA_SOURCE_NAME
              value: "postgresql://$(POSTGRES_USER):$(POSTGRES_PASSWORD)@localhost:5432/postgres?sslmode=disable"
      volumes:
        - name: postgres-data
          hostPath:
            path: /mnt/cephfs/docker/db/postgres/data
            type: Directory
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: db
  labels:
    app: postgres
  annotations:
    metallb.io/allow-shared-ip: "db"
spec:
  selector:
    app: postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
    - name: metrics
      port: 9187
      targetPort: 9187
  type: LoadBalancer
  loadBalancerIP: 10.1.1.57
